services:
  db:
    image: timescale/timescaledb:latest-pg18
    environment:
      POSTGRES_USER: keelwave
      POSTGRES_PASSWORD: keelwave
      POSTGRES_DB: keelwave
    ports:
      - "5432:5432"
    volumes:
      - keelwave_db:/var/lib/postgresql

volumes:
  keelwave_db:
