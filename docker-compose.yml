services:
  db:
    image: timescale/timescaledb:latest-pg18
    environment:
      POSTGRES_USER: vigil
      POSTGRES_PASSWORD: vigil
      POSTGRES_DB: vigil
    ports:
      - "5432:5432"
    volumes:
      - vigil_db:/var/lib/postgresql/data

volumes:
  vigil_db:
